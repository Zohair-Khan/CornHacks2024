import pygame


def map_screen():
    font = pygame.font.SysFont('arial', 30)
    level_button_positions = [
        (100, 100), (200, 100), (300, 100)]  # Example positions
    level_names = ['Level 1', 'Level 2', 'Level 3']
    # Draw map background and level buttons
    # Detect mouse click events
    # Check which level button is clicked and return the corresponding level
    # Return BATTLEFIELD_SCREEN with the selected level
    pass
